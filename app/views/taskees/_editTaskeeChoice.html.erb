<form action=<%= taskeeId(task) %> accept-charset="UTF-8" data-remote="true" method="post">
	<%= changeFormMethodOnBid(task) %>
	<input name="utf8" type="hidden" value="✓">
	<input type="hidden" name="taskee[task_id]" id="taskee_task_id" value=<%= task.id %>>
	<div class="input-group mb-3" <%=bidType(task)%>>
		<div class="input-group-prepend">
			<span class="input-group-text">$</span>
		</div>
		<input 
			value=<%=showBid(task) 
			%> 
			class="form-control col-5" 
			type="number" 
			step="0.01"
			name="taskee[bid]" 
			id="taskee_bid"> 
	</div>
	<%= buttonIfBid(task) %>
	<a 
		class="btn btn-warning titleFont grow"
		rel="nofollow" 
		data-method="delete" 
		<%=showUndoBid(task)%>
		href=<%= taskeeId(task) %> 
	>
	Undo Bid
	</a>
</form>